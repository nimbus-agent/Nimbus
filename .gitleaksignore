# Gitleaks suppressions for synthetic secret-redaction unit-test fixtures.
# These are NOT real secrets — they are deliberately constructed strings used
# to verify that scrubRedactedValuePatterns() in gateway-log-file.ts correctly
# strips Slack xoxb tokens and JWT fixtures from log output. The values match
# the shape of the corresponding redaction regexes by design.
#
# Adding the inline `// gitleaks:allow` comment in HEAD does not retroactively
# clear the historical commit, so we pin the fingerprints here.

b37e8d4b0abf44f8cd60b187499acea7f21914a7:packages/gateway/src/platform/gateway-log-redact.test.ts:generic-api-key:41
b37e8d4b0abf44f8cd60b187499acea7f21914a7:packages/gateway/src/platform/gateway-log-redact.test.ts:jwt:49

# Same situation in a planning doc that quotes a redactor unit-test —
# the JWT here is the canonical jwt.io sample (HS256 over "your-256-bit-secret"),
# embedded in the spec to show what the redactor must catch. Not a real token.
49889f354665f408a844b0466ab2b0c04b0e7a08:docs/superpowers/plans/2026-04-26-security-fixes-medium-tier.md:jwt:205

# Synthetic test fixture in the create-routing-runtime hybrid-mode tests.
# The literal "sk-from-env-456" was a placeholder API key value used to verify
# the OPENAI_API_KEY env var path; later rewritten to "env-present" (HEAD) but
# the historical commit retains the `sk-`-prefixed shape that trips gitleaks
# `generic-api-key`. The value was never a real OpenAI key — see commit message.
084cbfd69fd96f675ef0577ac670804d99ad9aa7:packages/gateway/src/embedding/create-routing-runtime.test.ts:generic-api-key:158

# Phase 1B test fixture false positives — scrubbed in HEAD but historical commits retain old strings.
# These were never real secrets; they were test fixtures matching the connector PAT shape.
fe9279eb7a68194d4ff848d6893e8f394f7347c9:packages/gateway/test/unit/ipc/connector-rpc-handlers/auth.test.ts:generic-api-key:204
cbc264f51fdc7082c86a4564ec37cfc9066ee260:packages/gateway/test/unit/ipc/connector-rpc-handlers/auth.test.ts:generic-api-key:216

# Phase 5 `nimbus security scan` (2026-05-21) — secret-patterns.test.ts asserts that
# the v1 PEM private-key header and Slack `xoxb-...` shape regexes fire on the
# canonical public header strings. These are NOT real keys — they're the literal
# string a real PEM/Slack value begins with, by design. HEAD carries inline
# `gitleaks:allow` markers; the historical commit must be pinned separately.
5f889abdbe5c795c94acffaf416e8dc83eb43adb:packages/gateway/src/security/secret-patterns.test.ts:slack-bot-token:123
5f889abdbe5c795c94acffaf416e8dc83eb43adb:packages/gateway/src/security/secret-patterns.test.ts:private-key:171
5f889abdbe5c795c94acffaf416e8dc83eb43adb:packages/gateway/src/security/secret-patterns.test.ts:private-key:173

# Same fixtures quoted inside the implementation plan doc — embedded test source.
317c6f12a4435a3497314d9a9ab8c8fc79d80819:docs/superpowers/plans/2026-05-21-nimbus-security-scan.md:slack-bot-token:185
317c6f12a4435a3497314d9a9ab8c8fc79d80819:docs/superpowers/plans/2026-05-21-nimbus-security-scan.md:private-key:235
317c6f12a4435a3497314d9a9ab8c8fc79d80819:docs/superpowers/plans/2026-05-21-nimbus-security-scan.md:private-key:237

# Phase 6 Slice 4 (2026-06-07) — the Ed25519 anchor signing seed is destructured from
# ensureAnchorKeypair(vault) in assemble.ts and resolved from the Vault at runtime. The
# renamed-destructuring shape trips gitleaks generic-api-key, but no literal key value
# appears in source. HEAD carries inline gitleaks-allow markers; the historical commits
# are pinned below.
ff3ab297ae365f7e2a499b21ffbb3111a79f9cc8:packages/gateway/src/platform/assemble.ts:generic-api-key:523
457ed8bf52502b20b0c4f9d2a142348639093075:packages/gateway/src/platform/assemble.ts:generic-api-key:660
